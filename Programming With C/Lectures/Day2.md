# day2

compiler problem -> no file.o
linker problem-> no exe

libaries are in the linker as a .o
#include "file1"
command -v gss.       command location

g++ -E.      preprocess

g++ -S.      compiler

void func()   all fit prototype

void func(void)     specifically tells that it has no params

cmake is the file that the ide calls to cimpile the program

cs clang tidy
render white space
clang format
sizeof() is an operator
size modifier [short,long,unsigned]
# coding rules

>return 0;

>int main (void){}
>No magic numbers   like int x=3;
>ALL vars must be initilazed
>#define x 1000              //replacement of a keyword
>printf("%d",x)      
>            %d      int 
>            %u      int
>            %f      double
>            %c      char
>            %s      [char]

>typedef short int bit16;      //replacement datatypes


# FAQ
create a file that has custom datatypes
know how to convert to binary
asscii table / utf-8 / utf-16
IEEE
hint: you have to open the assembly
hint : assemby in the debbuger


Assignment
1)define vs typedef
2)datatype for boolean in c named boolean
3)how does printf work? is that polymorphism? is a vardic funciton polymorphishm?